English
This plug-in is a post-screen special effect material, the effect is radial blur effect, simulating the visual effect of high-speed motion of animation elements.

Use the plugin:
Using plugins is very simple, just drag the "prefabs/RadialBlurCamera" into the scene to see the blurred effect. If you want to achieve blur on your camera, just add the "scripts/RadianBlurEffect" script as a component to your Just on the camera.

Adjust plugin parameters:
This plugin supports fuzzy parameter adjustment. The main parameters are as follows:
1 "DownSample", this parameter is used to downsample the current screen texture. By adjusting this parameter, the performance optimization of low-performance platforms such as mobile platforms can be achieved, but the low down-sampling rate will lead to the degradation of picture quality.

2 "RadianBlur", this parameter specifies the material required for the effect, the material position is material/m_radianblur.

3 "BlurCenterX", "BlurCenterY" These two parameters specify the blur center position of the screen, the range is 0-1.

4 "BlurRatio", this parameter specifies the fuzzy blur strength. The larger the coefficient, the more obvious the force.

5 "ClearDis", this parameter specifies the radius value of the non-blurred area from the blur center.

Support platform:
Various platforms

Contact information:
You are welcome to use the plugin, and if you have any questions during the process, you can send an email to Zgame_open@163.com.



中文
本款插件为后期屏幕特效材质，效果为径向模糊效果，模拟动画元素高速运动的视觉效果.

使用插件：
使用插件非常简单，只需要将prefabs/RadialBlurCamera拖入场景之中即可看到模糊后的效果.如果您想在自己的相机上实现模糊效果，只需将scripts/RadianBlurEffect脚本作为组件添加到您的相机上即可.

调节插件参数：
本款插件支持模糊参数调节，主要参数如下：
1 DownSample，该参数作用为将当前屏幕纹理进行降采样操作.通过调节该参数，可实现在移动平台等低性能平台的性能优化，但过低的降采样率会导致画面质量下降.
2 RadianBlur，该参数指定效果所需要的材质，材质位置为material/m_radianblur.
3 BlurCenterX，BlurCenterY这两个参数指定屏幕的模糊中心位置，范围都为0-1.
4 BlurRatio，该参数指定了模糊的模糊力度，系数越大力度越明显。
5 ClearDis，该参数指定了距模糊中心的非模糊区域的半径值.

支持平台：
各种平台

联系方式：
欢迎大家使用插件，并在使用过程中有任何疑问，可发送邮件至Zgame_open@163.com.


日本語
このプラグインはポストスクリーンの特殊効果素材で、効果は放射状のぼかし効果であり、アニメーション要素の高速モーションの視覚効果をシミュレートします。

プラグインを使用する：
プラグインを使用するのは非常に簡単です。 "prefabs / RadialBlurCamera"をシーンにドラッグすると、ぼやけたエフェクトが表示されます。あなたのカメラでぼかしを実現したい場合は、コンポーネントとして "scripts / RadianBlurEffect"スクリプトをちょうどカメラに追加してください。

プラグインパラメータを調整する：
このプラグインは、ファジーパラメータの調整をサポートします。主なパラメータは次のとおりです。
1 "DownSample"、このパラメータは、現在の画面テクスチャをダウンサンプリングするために使用されます。このパラメータを調整することによって、モバイルプラットフォームなどの低性能プラットフォームのパフォーマンスの最適化を達成できますが、低いダウンサンプリングレートは画質の劣化につながります。

2 "RadianBlur"、このパラメータはエフェクトに必要なマテリアルを指定し、マテリアルの位置はマテリアル/ m_radianblurです。

3 "BlurCenterX"、 "BlurCenterY"これらの2つのパラメータは、画面のブラー中心位置を指定します。範囲は0〜1です。

4 "BlurRatio"、このパラメータは、ぼかしぼかし強度を指定します。係数が大きければ大きいほど、力はより明確になります。

5 "ClearDis"、このパラメータは、ぼかし中心からの非ぼかし領域の半径値を指定します。

サポートプラットフォーム：
さまざまなプラットフォーム

連絡先：
プラグインを使用することは歓迎されます。処理中に質問がある場合は、Zgame_open@163.comに電子メールを送信することができます。